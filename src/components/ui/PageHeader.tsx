import type { ReactNode } from "react";
export function PageHeader({eyebrow,title,description,actions}:{eyebrow:string;title:string;description:string;actions?:ReactNode}) { return <div className="flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[.17em] text-[#a36f00]">{eyebrow}</p><h1 className="mt-2 text-3xl font-semibold tracking-[-.045em] md:text-[38px]">{title}</h1><p className="mt-2 max-w-2xl text-sm text-black/48">{description}</p></div>{actions && <div className="flex gap-2">{actions}</div>}</div>; }
