import { DatabaseZap,Send,ShieldCheck } from "lucide-react";
import { Badge } from "@/components/ui/Badge";
import { PageHeader } from "@/components/ui/PageHeader";
import { useCrmData } from "@/context/CrmDataContext";
export function QueuePage(){const{queue,loading}=useCrmData();return <div className="mx-auto max-w-[1100px] p-5 md:p-8 xl:p-10"><PageHeader eyebrow="WhatsApp seguro" title="Fila de pós-venda" description="Esta fila contém apenas registros reais aprovados pelo motor de elegibilidade."/><div className="mt-7 rounded-[22px] border border-black/[.07] bg-white p-5"><div className="flex items-center gap-2 rounded-xl bg-[#fff7dc] px-4 py-3 text-xs text-[#765506]"><ShieldCheck className="size-4"/><strong>DRY RUN permanece ativo até a homologação das integrações.</strong></div><div className="mt-4 space-y-3">{queue.map(item=><article key={item.id} className="rounded-2xl border border-black/[.07] p-4"><div className="flex items-start justify-between gap-3"><div><div className="flex items-center gap-2"><h3 className="text-sm font-semibold">{item.customerName}</h3><Badge tone="green">{item.brand}</Badge><Badge>{item.status}</Badge></div><p className="mt-3 text-xs leading-relaxed text-black/55">{item.messagePreview||"Mensagem aguardando montagem."}</p></div><span className="text-[10px] text-black/35">{item.purchaseAt?.toLocaleDateString("pt-BR")}</span></div></article>)}{!loading&&!queue.length&&<div className="grid place-items-center py-20 text-center"><DatabaseZap className="size-8 text-black/20"/><p className="mt-3 text-sm font-medium">Nenhuma mensagem real na fila.</p><p className="mt-1 text-xs text-black/40">A fila será criada depois da primeira importação elegível.</p></div>}</div></div><button disabled className="mt-5 flex w-full items-center justify-center gap-2 rounded-2xl bg-[#d7d7d2] py-4 text-sm font-semibold text-black/40"><Send className="size-4"/>Envio real indisponível durante a configuração</button></div>}
